pandas>=2.0
numpy>=1.24
PyYAML>=6.0
python-dotenv>=1.0
pytest>=7.4

# Opcional, só para backtest com ações da B3 via provider "yfinance":
yfinance>=0.2

# Opcional, só funciona no Windows com o terminal MetaTrader5 instalado,
# necessário para provider "mt5" (dados reais) e para live trading:
# MetaTrader5>=5.0.45 ; sys_platform == "win32"
